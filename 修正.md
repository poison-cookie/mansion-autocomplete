# 複数タブ利用時に手動登録候補が消失する不具合の調査・修正依頼 ## 概要 Tampermonkeyスクリプト（Mansion Name Autocomplete）において、同一サイトを複数タブで開いて運用していると、手動登録した候補名が消失することがある。 業務上、同じサイトを複数タブで開く運用は必須であり、複数タブ利用を前提とした実装へ改善したい。 --- ## 発生している症状 ### 症状 * 同じサイトを複数タブで開いている。 * 手動追加した候補名は一旦正常に登録される。 * 後日確認すると、今日追加した候補がすべて消えていることがある。 * 昨日以前に登録した候補は残っている。 * 「候補が1件だけ残る」状態になることもある。 * CSVインポートしたデータは残っていることが多い。 ### 発生条件（推定） 例：
朝

タブA起動
sitePhrases = [A]

タブB起動
sitePhrases = [A]

↓

タブAで C を追加
保存 → [A,C]

↓

タブBは古い [A] を保持したまま

タブBで D を追加
保存 → [A,D]

結果：
C が消失
さらに、
タブA
[A,B,C,D]

タブB
[A]

↓

最後にタブBが保存

↓

保存データ = [A]
となり、 * 今日追加した候補が全部消える * 昨日までの候補だけ残る という実際の症状と一致する。 --- ## 原因の推定 現在の実装では、スクリプト起動時に
javascript
sitePhrases = readValue(SITE_PHRASES_KEY, {});
でデータを取得し、その後はメモリ上の sitePhrases を使い続けている。 現在の保存処理：
javascript
function saveCurrentSitePhrases(nextPhrases) {
    const cleaned = uniqueNonEmpty(nextPhrases);
    if (cleaned.length) {
        sitePhrases[siteKey] = cleaned;
    } else {
        delete sitePhrases[siteKey];
    }
    writeValue(SITE_PHRASES_KEY, sitePhrases);
}
このため、別タブで更新された最新データを知らないまま、古い状態で全体を上書き保存してしまっている可能性が高い。 --- ## GM_setValue()について 保存処理は * GM_getValue() * GM_setValue() を使用しており、localStorageではない。 そのため、 * Cookie削除 * localStorage.clear() * キャッシュ削除 によるデータ消失ではないと考えられる。 --- ## 修正方針 ### ① 保存直前に最新データを再取得（必須） 保存処理の前に、
javascript
sitePhrases = readValue(SITE_PHRASES_KEY, {});
を実行し、必ず最新状態を取得する。 さらに、
javascript
const current =
    Array.isArray(sitePhrases[siteKey])
        ? sitePhrases[siteKey]
        : [];

sitePhrases[siteKey] =
    uniqueNonEmpty([
        ...current,
        ...cleaned
    ]);
のように、既存データとマージして保存する。 --- ### ② タブ切替時に同期（推奨）
javascript
window.addEventListener("focus", () => {
    sitePhrases = readValue(
        SITE_PHRASES_KEY,
        {}
    );
});
タブがアクティブになったタイミングで最新データを取得する。 --- ### ③ GM_addValueChangeListener()でリアルタイム同期（推奨） Tampermonkeyの
javascript
GM_addValueChangeListener()
を利用し、
javascript
GM_addValueChangeListener(
    SITE_PHRASES_KEY,
    function(name, oldValue, newValue, remote) {
        if (remote) {
            sitePhrases = newValue || {};
        }
    }
);
を実装する。 これにより、 * タブAで追加 * タブB、Cへ即時反映 が可能になる。 --- ## 注意事項 今後、手動登録候補の削除機能を実装する予定がある場合は、 単純なマージ処理だけでは、 古いタブが削除済みデータを復活させる可能性がある。 その場合は、 * 差分更新 * タイムスタンプ管理 * イベント同期 なども含めて設計を検討したい。 --- ## 別件（参考） 長谷工ライブネットで表示される > 東阪フラグが不正です については、 * ログアウト機能が存在しない * 普段はタブを閉じずに運用している * URLを直接開いた時に発生 * CSVデータ・Tampermonkeyデータは保持されている ことから、 スクリプトではなく、 サイト側のセッション・画面遷移管理による挙動である可能性が高いため、本件とは切り分けて考える。